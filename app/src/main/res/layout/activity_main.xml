<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#F5F5F5">

    <TextView
        android:id="@+id/tvResult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:padding="20dp"
        android:text="0"
        android:textSize="48sp"
        android:textColor="#212121" />

    <EditText
        android:id="@+id/etNum1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="First Number"
        android:inputType="numberDecimal"
        android:importantForAutofill="no" />

    <EditText
        android:id="@+id/etNum2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Second Number"
        android:inputType="numberDecimal"
        android:importantForAutofill="no" />

    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="2"
        android:layout_marginTop="20dp">

        <Button android:id="@+id/btnAdd" android:layout_columnWeight="1" android:text="+" />
        <Button android:id="@+id/btnSub" android:layout_columnWeight="1" android:text="-" />
        <Button android:id="@+id/btnMul" android:layout_columnWeight="1" android:text="×" />
        <Button android:id="@+id/btnDiv" android:layout_columnWeight="1" android:text="÷" />
    </GridLayout>

    <Button
        android:id="@+id/btnClear"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="CLEAR"
        android:layout_marginTop="10dp"
        android:backgroundTint="#D32F2F" />

</LinearLayout>